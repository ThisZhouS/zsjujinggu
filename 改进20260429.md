1.首先数据的大部分功能没有问题。但是存在数据确实，牛散应该不止那么少。**牛散**应该从真实的**公司十大流通股东**中筛选计算聚合获取。
2.其次涨幅榜，涨跌统计的数据不够完整。特别是涨跌统计，一年的时间范围里应该不可能最多只有两次的跌停，涨停。
3.高管增持使用的是张三李四王五这种示例数据，而非真实数据。
以上均为数据上的缺失与不完整，不正确。根据实际的数据库与数据接口数据，而非虚假的不真实的数据。

前端页面存在字段筛选无效的情况。
牛散的持股页面还缺少一个股票的筛选。

**示例模仿网站：www.tetegu.com**

需要做的事：
1.修复数据与数据筛选计算聚合程序。
2.使程序能够使用真实可靠有效的数据。
3.重构重新设计前端页面。
4.朝tetegu网站的功能与数据复制模仿。

我们的核心思想：在tetegu原有的基础上更进一步。
现在在做的：实现tetegu原有基础。

---

## 2026-05-06 本轮修复任务

说明：当前判断以“接口本身可用，但参数、口径、报告期选择存在问题”为主，不先推翻接口，先修正调用参数与业务聚合逻辑。

### 已确认根因

1. 多个榜单直接取“最大报告日期”，落到了只有个位数或几十条记录的零碎更新日期，而不是最新完整快照期。
2. 牛散增持、减持、新进页面后端返回字段被裁剪，前端也缺少牛散主体信息与更完整的筛选展示。
3. 十大增持模块当前使用的报告期口径不稳定，需要改成“最新有效快照期 vs 上一有效快照期”。
4. 高管增持模块现有数据不可直接信任，需要先校正参数与数据来源，再改为真实高管成员关联的持仓变动榜。
5. 文章管理仅支持手工填写封面 URL，缺少文件上传能力。

### 本轮任务清单

| 状态 | 任务 | 说明 |
| --- | --- | --- |
| 进行中 | T1 建立任务与验收文档 | 记录根因、实施顺序、验收口径 |
| 待办 | T2 修复涨幅榜页面空数据问题 | 先校验前端取数与页面状态，再修正必要参数 |
| 待办 | T3 修复牛散增持/减持/新进不完整问题 | 统一改为最新有效持仓快照期，并补足牛散字段 |
| 待办 | T4 重做十大增持模块 | 基于真实自然人流通股东数据，按有效快照期比较 |
| 待办 | T5 重做高管增持模块 | 基于真实高管成员与自然人持仓交叉结果生成榜单 |
| 待办 | T6 增加文章上传功能 | 后端上传接口 + 静态访问 + 管理页上传入口 |
| 待办 | T7 重启并逐项验证 | 验证接口、页面、上传链路与数据完整性 |

### 验收标准

1. 涨幅榜页面在前端实际可见数据，不再出现空表。
2. 牛散增持、减持、新进页面能显示牛散姓名、股票、变化数量、变化比例、持仓市值、报告期。
3. 十大增持页面使用最新有效快照期，且榜单规模不再被稀疏日期截断。
4. 高管增持页面不再展示伪造样本姓名，改为真实高管成员关联结果。
5. 后台文章管理支持上传封面图，上传后可直接回填并在文章详情页显示。
